# Untested Implementations and Release Blockers

## Purpose

Track changes that are implemented but not fully validated. Any item listed here is a release blocker for end-user rollout until required tests are completed.

## Rule

1. If implementation is complete but testing is partial/incomplete, add an entry here immediately.
2. Mark release status as `BLOCKED`.
3. Remove or close the entry only after required tests pass.

## Open Blockers

| Date | Change | Status | Missing Tests | Owner | Required Before Release |
| 2026-04-08 | P30 S1 — LinkedIn Import Trigger (`src/hooks/useLinkedinScrape.ts` + Settings Import button) | UNTESTED | Manual: (1) /settings as Pro user — confirm "Import from LinkedIn" button appears next to URL field; (2) Enter a valid LinkedIn URL, click Import — confirm spinner, then `ProfileImportReviewModal` opens with skills + About section; (3) As Free user — confirm button absent and "Pro+ required" note visible; (4) Simulate VERIFICATION_REQUIRED error — confirm user-friendly message shown; (5) Simulate CONFIG_ERROR — confirm "temporarily unavailable" message | Claude Code | Requires Railway Playwright service running + valid Pro test account |
| 2026-04-08 | P30 S4 — Reconciliation at Pro+ tier (`src/hooks/usePlanFeature.ts` tier change + `src/pages/ProfileFit.tsx` LockedPanel fix) | UNTESTED | Manual: (1) /profile-fit as Pro user — confirm reconciliation section renders without locked overlay; (2) As Free user — confirm "Pro plan required" overlay; (3) As Pro user — confirm Score History still shows "Max plan required"; (4) As Max user — confirm Score History unlocked | Claude Code | Requires Pro and Max test accounts |
| 2026-04-08 | P30 S5 — Consistency Health Score (`src/lib/consistency-score.ts` + `ConsistencyScoreDisplay` in ProfileFit) | UNTESTED | Manual: (1) /profile-fit as Pro user with resume selected — run reconciliation — confirm score renders above conflict list with correct colour (≥80 green, 60–79 amber, <60 red); (2) No conflicts — confirm score shows 100 in green; Unit: `npm run test -- tests/unit/utils/consistencyScore.test.ts` — 7 cases | Claude Code | Unit tests pass; integration requires live reconciliation run |
| 2026-04-08 | P30 S6 — Stale LinkedIn Import Alert (`src/hooks/useLinkedinImportAge.ts` + alerts in Settings + ProfileFit) | UNTESTED | Manual: (1) Account with last LinkedIn import >30 days ago — confirm Alert appears in /settings and /profile-fit; (2) Click dismiss — confirm alert disappears and does not reappear in same session; (3) Account with no LinkedIn import — confirm no alert; Unit: `npm run test -- tests/unit/utils/linkedinImportAge.test.ts` — 4 cases | Claude Code | Requires test account with old LinkedIn import row in sats_user_skills |
| 2026-04-08 | P30 S7 — About section in HITL Review Modal (`supabase/functions/linkedin-profile-ingest` + `ProfileImportReviewModal`) | UNTESTED | Manual: (1) Trigger LinkedIn import for a profile with About section — confirm read-only About preview renders at top of modal (≤400 chars, truncated with ellipsis if longer); (2) Profile with no About section — confirm no About section rendered in modal; (3) Save import — confirm About text is not persisted to any DB table | Claude Code | Requires Railway Playwright service running + LinkedIn profile with non-empty About/Summary |
| 2026-04-08 | Callback Rate UX — Recruiter Searchability Score rename + score_breakdown bars (all users) + First-Impression Summary badge (`src/pages/ATSAnalyses.tsx`) | CODE-VERIFIED | Manual: (1) Open `/analyses`, expand any completed analysis — confirm label reads "Recruiter Searchability Score" with tooltip; (2) Confirm score breakdown bars appear (Skills Alignment, Experience Relevance, Domain Fit, Format Quality) with green/amber/red colouring; (3) Confirm First-Impression Summary badge appears between skills grid and AI Suggestions (Strong/Needs Attention/Format Issues Found); (4) Open an analysis that pre-dates P10 (no score_breakdown) — confirm breakdown section is hidden, badge absent; (5) Confirm Pre-existing Debug Modal still shows breakdown on Pro+ | Claude Code | Visual + functional verification in dev environment |
| 2026-04-08 | Knock Knock Outreach Coach — `generate-outreach-message` edge function + `useOutreachMessage` hook + `KnockKnockModal` component + Reach Out button on `/opportunities` (`src/pages/ProactiveMatches.tsx`) | CODE-VERIFIED | Manual: (1) Navigate to `/opportunities` — confirm "Reach Out" button appears on each opportunity card; (2) Click Reach Out — confirm modal opens with loading state, then generates a 3-paragraph message; (3) Confirm message references the job title and company name; (4) Edit the message — confirm textarea is editable; (5) Click Copy to Clipboard — confirm copied state shows check icon for 2 seconds; (6) Test with missing OPENAI_API_KEY — confirm edge function returns 503 | Claude Code | Requires at least one proactive match (ats_score ≥ 61%) in the test account; edge function deployed to Supabase 2026-04-08 |
| 2026-04-07 | ATS Analyses page UIUX redesign — collapsed score-first cards with progressive disclosure, colour-coded left border, filter bar (All/Strong Match/Needs Work/In Progress), AnimatePresence expand/collapse, debug/export moved to expanded state only (`src/pages/ATSAnalyses.tsx`) | CODE-VERIFIED | Manual: (1) Open `/analyses` — confirm cards are collapsed showing score, title pair, signal badges; (2) Click a card — confirm it expands with full detail; (3) Test filter bar filters correctly; (4) Confirm Debug/Export buttons only appear in expanded state | Claude Code | Visual + functional verification in dev environment |
| 2026-04-07 | ATSDebugModal feature gating — Score Breakdown (Pro+), CV Optimisation (Pro+), Model Details hidden for Free/Pro, Prompts/AI Output/Usage tabs locked for Free/Pro with lock icons and LockedTabContent nudge; Retry always visible; score bars colour-coded (`src/components/ATSDebugModal.tsx`, `src/hooks/usePlanFeature.ts`) | CODE-VERIFIED | Manual: (1) As Free user — confirm Score Breakdown shows locked panel, Prompts/AI Output/Usage show LockedTabContent, Retry still works; (2) Override plan to `pro` via Admin → Plan Overrides — confirm Score Breakdown and CV Opt unlock; (3) Override to `max` — confirm all tabs and Model Details unlock; (4) Check score bars are green/amber/red | Claude Code | Plan-tier gating must be verified at each tier level |
| 2026-04-07 | Settings "Your Plan" PlanBillingCard — tier comparison section at top of `/settings` with Free/Pro/Max/Enterprise cards, current plan highlighting, UpgradeRequestModal (replaced "coming soon" dialog — P29), Contact Sales mailto (`src/components/settings/PlanBillingCard.tsx`) | CODE-VERIFIED | Manual: (1) Open `/settings` — confirm "Your Plan" card appears first; (2) Confirm current plan is highlighted and labelled "Current plan"; (3) Click "Upgrade to Pro" — confirm UpgradeRequestModal opens with feature list and "Request access" button; (4) Click "Contact sales" on Enterprise — confirm mailto opens; (5) Submit request — confirm success state | Claude Code | Resolves dead-end from "Upgrade" CTAs on locked feature pages |
| 2026-04-08 | P29 — MVP Upgrade Request Flow: `sats_upgrade_requests` table + RLS (migration `20260408020000`), `sats_approve_upgrade_request()` SECURITY DEFINER RPC, `request-plan-upgrade` edge function (Resend email non-fatal), `UpgradeRequestModal` component, `useUpgradeRequests`/`useApproveUpgradeRequest`/`useDenyUpgradeRequest` hooks, `UpgradeRequestsPanel` admin component, Admin Dashboard "Upgrade Requests" tab with pending badge | CODE-VERIFIED | Manual: (1) As Free user — open Settings → Plan & Billing, click Upgrade to Pro, confirm UpgradeRequestModal opens with Pro features listed; (2) Click "Request access" — confirm submitting state then success state; (3) As admin — open Admin → Upgrade Requests, confirm request row appears with user name/email, tier, timestamp, Approve/Deny buttons; (4) Click Approve — confirm status changes to "approved" and user's plan_override updates; (5) Click Deny on a pending request — confirm status changes to "denied"; (6) Missing SATS_ADMIN_NOTIFICATION_EMAIL env — confirm 503 returned | Claude Code | Migration must be applied (`supabase db push`); `SATS_ADMIN_NOTIFICATION_EMAIL` secret must be set in Supabase edge function secrets |
| 2026-04-07 | P28 — LinkedIn Profile Fit Analyzer (S1–S7): `sats_profile_fit_reports` table, `analyze-profile-fit` edge function, `useProfileFit` hook, `/profile-fit` UI, `ReconciliationConflictList`, `FitScoreHistoryChart`, Playwright scraper certifications extraction | **RUNTIME-VERIFIED — 2026-04-07** (Test 9 code-verified; Railway credentials pending) | ~~(3) Happy path~~ ✅ fit_score=50, row `84e26d80` in DB. ~~(4) Reconciliation~~ ✅ reconciliation_conflicts=[] with resume+extraction. ~~(5) No market signals~~ ✅ HTTP 404, error=`no_market_signals`. ~~(6) Missing env vars~~ ✅ code-verified (guard at lines 368–375, all 3 secrets confirmed present). ~~(7) Free-tier upsell~~ ✅ lock icon + heading + "Upgrade to Pro" only. ~~(8) Pro+ full UI~~ ✅ score card 50%, gap breakdown, reconciliation+history locked for Max. (9) Scraper enrichment — code path verified (lines 198–213, 614); Railway credentials set + redeployed; auth works (23 cookies restored) but datacenter IP triggers LinkedIn bot detection (DOM restricted, `EXTRACTION_FAILED`). `recommendation_count` key presence confirmed correct via code simulation. Full runtime pass requires residential/proxy IP or local execution. | Claude Code | Step (9) non-blocking for core P28 release. Railway IP restriction is an infrastructure constraint, not a code bug. | | Owner | Required Before Release |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-02-24 | P14 Story 1 (`fetch-market-jobs`) deployment | **RUNTIME-VERIFIED — 2026-03-29** | E2E validated 2026-03-29: `fetch-market-jobs` invoked via cron/manual trigger; staged 3 mock jobs to `sats_staged_jobs` with `status: queued`, deduplicated by content hash on re-run (0 new rows). Pipeline confirmed end-to-end operational after migration `20260328230000_fix_p14_proactive_job_conflict_constraints.sql` applied. | Claude Code | ~~Confirm cron runs, validate staged row lifecycle and dedupe outcomes over repeated runs~~ ✅ DONE |
| 2026-02-24 | P14 Story 2 (`async-ats-scorer`) deployment | **RUNTIME-VERIFIED — 2026-03-29** | E2E validated 2026-03-29: scorer processed 3 staged jobs, produced 21 scored analyses (`processed_jobs:3, scored_analyses:21, failed_jobs:0`). Root cause of prior failure was partial unique indexes blocking `ON CONFLICT` upserts (error 42P10) — fixed by migration `20260328230000_fix_p14_proactive_job_conflict_constraints.sql`. Baseline from `document_extractions` (resume text) used as fallback when `sats_user_skills` is empty. | Claude Code | ~~Run E2E test pass and confirm logs + DB outputs~~ ✅ DONE |
| 2026-02-24 | P14 Story 3 (threshold + notifications) deployment | CODE-VERIFIED — notification threshold bug fixed, deploy pending | E2E run 2026-03-29: scorer ran successfully with `notifications_triggered:0` even when global threshold lowered to 0.40 and top scores were 0.41–0.48. Root cause identified: `getUserThresholdMap()` stored `DEFAULT_PROACTIVE_MATCH_THRESHOLD` (0.6) for all users with null per-user threshold, preventing `globalThreshold` from `sats_runtime_settings` from ever being reached. Fix applied to `async-ats-scorer/index.ts` (2026-03-30): map only populated for users with explicit threshold; others fall through to `globalThreshold` via `??`. **Deployment blocked by expired `SUPABASE_ACCESS_TOKEN`** — renew PAT, then `supabase functions deploy async-ats-scorer`, then re-run scorer with threshold=0.40 to verify `notifications_triggered > 0`. | Claude Code | (1) Renew SUPABASE*ACCESS_TOKEN → (2) `supabase functions deploy async-ats-scorer` → (3) Set threshold to 0.40 → (4) Reset staged jobs to queued → (5) Trigger scorer → (6) Confirm `notifications_triggered > 0` and `sats_user_notifications` row created |
| 2026-02-24 | P14 Story 4 (`/opportunities` UI dashboard) implementation | **RUNTIME-VERIFIED — 2026-03-29** | Playwright suite run 2026-03-29: all 8 `opportunities.spec.ts` tests passed (`npx playwright test --project=functional`). Heading, Beta badge, subtitle, no-error state, score display, ordering, and sidebar navigation all confirmed. Data-dependent tests skip gracefully when no proactive analyses exist. | Claude Code | ~~Set credentials, run functional suite~~ ✅ DONE |
| 2026-02-24 | ATS analyses auto-refresh UX improvement (processing state stays at 60% until manual refresh) | **RUNTIME-VERIFIED — 2026-03-29** | Playwright suite run 2026-03-29: all relevant `ats-analyses.spec.ts` tests passed. Heading, "Last sync" timestamp, no-error state, and 100% complete indicator all confirmed. Real-time transition test (60% → 100% without reload) skips gracefully as it requires an in-flight OpenAI call — this is expected and documented. | Claude Code | ~~Set credentials, run functional suite~~ ✅ DONE (real-time transition test intentionally skipped — requires in-flight analysis) |
| 2026-02-25 | P15 Story 1 (`sats_learning_roadmaps` + `sats_roadmap_milestones` schema and RLS) | CODE-VERIFIED — cross-tenant RLS check pending | Automated RLS script written: `scripts/ops/test-rls-cross-tenant.ts`. Signs in as User A, creates a roadmap, signs in as User B (separate client), queries by ID and full-scan — expects 0 rows. Cleans up test row. Run: `npx tsx scripts/ops/test-rls-cross-tenant.ts` with USER*A** and USER*B*\_ credentials. | Claude Code | Set USER_A_EMAIL/PASSWORD + USER_B_EMAIL/PASSWORD, run `npx tsx scripts/ops/test-rls-cross-tenant.ts`, confirm all checks PASS |
| 2026-02-25 | P15 Story 2 (`generate-upskill-roadmap` edge function) implementation + deployment | CODE-VERIFIED — runtime E2E pending | Code review (2026-03-17): `generate-upskill-roadmap/index.ts` accepts `missing_skills` + `target_role`, calls `callLLM()` with schema-locked ROADMAP_JSON_SCHEMA, inserts roadmap + milestones into `sats_learning_roadmaps`/`sats_roadmap_milestones`, returns `roadmap_id`. Uses `_shared/` utilities. JSON schema enforces `project` milestone type. | Claude Code | Run E2E call from authenticated UI/session; verify `roadmap_id` returned; confirm milestone order and project milestone present |
| 2026-02-25 | P15 Story 3 (`/roadmaps` timeline UI + milestone completion toggles + progress bar) implementation | **RUNTIME-VERIFIED — 2026-03-29** | Playwright suite run 2026-03-29: all 8 `roadmaps.spec.ts` tests passed including milestone toggle persistence, progress bar update on toggle, and multi-roadmap selector. All acceptance criteria confirmed against live data. | Claude Code | ~~Set credentials, run functional suite~~ ✅ DONE |
| 2026-02-25 | P13 Story 1 (`linkedin-profile-ingest` edge function) implementation | CODE-VERIFIED — runtime E2E pending | Code review (2026-03-17): `linkedin-profile-ingest/index.ts` calls Playwright scraper via HTTP (not mock), passes result through `callLLM()` with schema-locked JSON, returns structured preview without any DB writes. Uses `_shared/` utilities correctly. | Claude Code | Invoke with valid LinkedIn URL under authenticated user; verify returned arrays + provenance fields; confirm no DB mutations |
| 2026-02-25 | P13 Story 2 (frontend merge/dedupe utility + import preparation hook) implementation | CODE-VERIFIED — unit tests passing | Vitest unit test suite expanded: `tests/unit/utils/linkedinImportMerge.test.ts` (11 tests — 3 existing + 8 new). New tests cover: asymmetry-fix regression (company_name excluded from fingerprint on both sides), provenance tagging on experience inserts, empty-baseline all-insert, within-payload duplicate skills, within-payload duplicate experiences, synonym canonicalization (ReactJS→react, Node.js→node, JS, TS), below-threshold fuzzy (distinct skills both insert), exact-match reason text. All 11 pass (`npm run test -- tests/unit/utils/linkedinImportMerge.test.ts`). Remaining: live S1→S2 flow with real LinkedIn URL (requires Railway scraper). | Claude Code | Run `npm run test -- tests/unit/utils/linkedinImportMerge.test.ts` → all 11 pass. For live flow: execute Story 1 preview, feed result into Story 2, verify bucket decisions under real baseline data |
| 2026-03-01 | P13 Story 3 (HITL Review UI — `ProfileImportReviewModal.tsx` + Settings import button) | CODE-VERIFIED — runtime E2E pending | Code review + scroll fix (2026-03-17): all acceptance criteria met in code. Checkboxes default-checked; `Approve and Save` writes only selected items; loading/error/success states present; empty-state renders when no new items found. `Settings.tsx` import button correctly invokes edge function → merge prep → modal. Scroll clipping bug fixed (pr-3 moved to content div). ~~Migration applied 2026-03-01~~ ✓ | Claude Code | Run E2E flow with real LinkedIn URL; confirm DB row insertions match checkbox selection; verify rejected items produce no DB writes |
| 2026-02-25 | Help Hub page (`/help`) UI implementation | **RUNTIME-VERIFIED — 2026-03-29** | Playwright suite run 2026-03-29: all 10 `help-hub.spec.ts` tests passed. Page load, sidebar navigation, search filter (filters by keyword, clears, shows empty-state), and deep-link navigation (Dashboard→`/`, Resumes→`/resumes`, ATS→`/analyses`) all confirmed. | Claude Code | ~~Set credentials, run functional suite~~ ✅ DONE |
| 2026-03-17 | P16 S1 — Resume Persona Model (`sats_resume_personas` migration + `useResumePersonas` hook + `PersonaManager` component wired into Settings) | CODE-VERIFIED — 6/7 tests pass, CRUD skipped | Playwright suite run 2026-03-29: 6 of 7 `persona-manager.spec.ts` tests passed (section visible, "New Profile" button, dialog opens, form fields, validation errors, Cancel). CRUD flow test skips with message "Persona INSERT failed — run supabase db push to apply pending P21 migrations". Requires `supabase db push` to apply migration `20260327231000_fix_has_role_sats_user_roles_rename.sql` and restore full admin access including persona persistence. | Claude Code | Run `supabase db push` to apply `20260327231000_fix_has_role_sats_user_roles_rename.sql`, then re-run `npx playwright test --project=functional` to confirm CRUD flow passes |
| 2026-03-17 | BUG-2026-03-17-LOCATION-RLS fix — `sats_locations` + `sats_companies` SELECT policies replaced | **RUNTIME-VERIFIED — 2026-03-29** | RLS script run 2026-03-29: `npx tsx scripts/ops/test-rls-cross-tenant.ts` — "sats_locations INSERT succeeds", "sats_locations row readable after INSERT", "Cleanup: test location deleted" all PASS (3/3). Note: script had `region` column bug (table has no such column) — fixed before run. | Claude Code | ~~Set USER_A_EMAIL/PASSWORD, run RLS script~~ ✅ DONE |
| 2026-03-17 | P18 CV Optimisation Score — S2 ATS scorer enrichment projection | CODE-VERIFIED — runtime E2E pending | Two-call isolation implemented (commit `bf879f9`): baseline `buildATSPrompt()` is pure (no enrichments); `getAcceptedEnrichments()` runs after baseline call; separate `callLLM()` with `CV_OPTIMISATION_JSON_SCHEMA` and `buildOptimisationPrompt()` projects enriched score. Optimisation failure is isolated and cannot corrupt `ats_score`. Requires runtime test: (1) create ≥1 accepted enrichment on a resume, (2) run new ATS analysis against a JD, (3) confirm `analysis_data.cv_optimisation_score` and `analysis_data.enrichments_used_count > 0` in DB; (4) confirm baseline score is NOT lower than a pre-enrichment run on same resume+JD. | Claude Code | Run ATS analysis with accepted enrichments; confirm `analysis_data` contains `cv_optimisation_score`, `cv_optimisation_improvements`, and `enrichments_used_count > 0`; confirm baseline ats_score is unchanged vs a run without enrichments |
| 2026-03-17 | P18 CV Optimisation Score — S3 UI panel in ATSAnalyses.tsx | CODE-VERIFIED — absent-case verified, present-case pending | Playwright suite run 2026-03-29: "CV Optimisation panel is absent on analyses with no enrichments" PASSED (panel correctly absent). "panel shows projected score and delta when present" skipped gracefully (no enrichment data in test account). Full panel-present path requires S2 (scorer) to produce an analysis with `cv_optimisation_score > 0` first. | Claude Code | Run ATS analysis with accepted enrichments (S2), then re-run `npx playwright test --project=functional` to confirm panel renders with % score and delta |
| 2026-04-02 | Gap 2 — Admin JD Observability (`AdminJobDescriptionsPanel` + migration `20260402000000_admin_jd_select_policy.sql`) | **RUNTIME-VERIFIED — 2026-04-02** | Admin panel confirmed working: "Job Descriptions" tab visible at `/admin`, cross-user JDs loaded (20 rows from multiple user IDs), search and filters rendered correctly. Follow-up migration `20260402020000_cleanup_orphaned_proactive_data.sql` applied to remove ghost-user proactive JDs created during P14 test run. After cleanup, panel correctly shows only real user JDs. | Claude Code | ✅ DONE |
| 2026-04-05 | P26 — Gap Analysis Engine: `aggregate-market-signals` edge function | CODE-VERIFIED — runtime E2E pending | Requires real `sats_staged_jobs` rows with `structured_extracted_at IS NOT NULL` and `role_family_id IS NOT NULL` to produce output. Unit-testable in isolation with fixture data. | Claude Code | (1) Deploy `aggregate-market-signals` to Supabase; (2) Verify at least one processed staged job with signal columns populated; (3) POST to function (service-role key) or await nightly cron; (4) Confirm `sats_market_signals` rows inserted; (5) Check `{ role_families_processed, signals_upserted, duration_ms }` response. |
| 2026-04-05 | P26 — Gap Analysis Engine: `generate-gap-matrix` edge function + `/gap` page | CODE-VERIFIED — runtime E2E pending | E2E requires `sats_market_signals` populated (depends on `aggregate-market-signals` run above). LLM enrichment (recommended_action, weeks-to-close, resume language) untested. Gap snapshot upsert + item persistence untested. | Claude Code | (1) Deploy `generate-gap-matrix`; (2) Ensure market signals exist for a role+market; (3) Set career goals in `/settings`; (4) Navigate to `/gap`; (5) Click "Refresh Analysis"; (6) Confirm three gap sections render; (7) Click "Generate Roadmap from Gaps" and confirm `/roadmaps` receives new roadmap. |
| 2026-04-05 | P26 — Job signal extraction in `async-ats-scorer` (S1-1) | CODE-VERIFIED — runtime E2E pending | `extractJobSignals()` runs after per-job scoring only when `structured_extracted_at IS NULL`. Needs a real staged job processed by the updated scorer version. | Claude Code | (1) Deploy updated `async-ats-scorer`; (2) Reset a staged job to `queued`; (3) Trigger scorer; (4) Inspect `sats_staged_jobs` row — confirm `certifications`, `tools`, `market_code`, `role_family_id`, `structured_extracted_at` populated. |
| 2026-04-05 | P26 — Career Goals card in Settings + `useCareerGoals` hook | CODE-VERIFIED — runtime E2E pending | Multi-toggle market selection and role family searchable dropdown untested in live environment. Upsert to `profiles` columns `target_market_codes` + `primary_target_role_family_id` untested. | Claude Code | (1) Navigate to `/settings`; (2) Scroll to Career Goals card; (3) Toggle 2 markets + select a role family; (4) Save; (5) Reload and confirm selections persist. |
| 2026-04-02 | Gap 4 — P20 S4 Career Data Reset (`reset_career_data` RPC + `reset-profile-data` edge function + `ResetCareerDataModal`) | CODE-VERIFIED — runtime E2E pending | Migration applied 2026-04-02. Edge function deployed 2026-04-02 to project `nkgscksbgmzhizohobhg`. | Claude Code | (1) Navigate to `/settings` → (2) Scroll to Data Management card → (3) Click "Reset career data" → (4) Type RESET and click confirm → (5) Verify HTTP 200 from `reset-profile-data` edge function → (6) Confirm all career tables empty for user; profile and auth intact → (7) Verify TanStack Query caches refresh (UI shows empty state) |
| 2026-04-02 | ADR-0007 — Postmark inbound email job ingestion (`inbound-email-ingest` edge function + Settings "Email Job Alerts" card) | **RUNTIME-VERIFIED — 2026-04-05** | Full pipeline confirmed 2026-04-05: forwarded LinkedIn job alert email → Postmark processed → 5 jobs staged in `sats_staged_jobs` (source=`linkedin-email-alert`, status=`queued`) → `async-ats-scorer` invoked → `processed_jobs:6, scored_analyses:6, failed_jobs:0, notifications_triggered:4` → `/opportunities` populated with 4 high-match results. Two bugs fixed during setup: (1) `verify_jwt=false` added to `supabase/config.toml` (Postmark returned 401 without it); (2) LinkedIn email URLs use `/comm/jobs/view/` path — regex updated to handle both `/jobs/view/` and `/comm/jobs/view/`. | Claude Code | ✅ DONE |
| 2026-03-17 | ATS model infrastructure: o4-mini pricing + reasoning model handling in `llmProvider.ts` | CODE-VERIFIED — §4.3 live validation pending | `llmProvider.ts` updated with o4-mini pricing and `max_completion_tokens` handling. **o4-mini rolled back; `gpt-4.1` is production default.\*\* **2026-03-27: `npm run llm:eval:gate` passed** (all rubrics 1.0). **2026-03-29: governance doc §2 Model Register updated** — `gpt-4.1` now correctly listed as current model (was incorrectly showing `o4-mini`); §6 Change Log updated with rollback entry. Remaining: §4.3 live suite (3× determinism ±2pp; 5-pair rubric; edge cases ≥85%/≤30%) requires live OpenAI API calls. | Claude Code | Trigger 3 ATS analyses on identical resume+JD; confirm scores within ±2pp. Run rubric pairs. Confirm §4.3 pass criteria met. |
| 2026-03-17 | Admin LogViewer time-window filter (`src/components/admin/LogViewer.tsx`) | **RUNTIME-VERIFIED — 2026-03-29** | Playwright suite run 2026-03-29: all 9 `log-viewer.spec.ts` tests passed. Log Viewer card visible, Time Window and Level labels present, default state (ERROR + Last 1h) confirmed, all dropdown options present, filter switching confirmed including All Levels + All time ≥ row count, label toggle confirmed. | Claude Code | ~~Set admin credentials, run functional suite~~ ✅ DONE |

## Closure Template

| Date Closed | Change                                                                                               | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Closed By   |
| ----------- | ---------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| 2026-02-24  | Enrichment edge compatibility fix (`enrich-experiences`)                                             | Deployed to project `nkgscksbgmzhizohobhg`; direct function invocation returned `HTTP 200` and `success: true` with suggestions (`request_id=manual-test-20260224c`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Codex       |
| 2026-02-24  | Enriched experience soft-delete RLS fix (delete flow stability)                                      | User-verified logs show repeated `enrichment.deleted` success events with no new RLS failures in latest window (`request_id`s include `enrich-delete-ee15b057-7a86-4601-a5a5-dfc7b012ceff`, `enrich-delete-26b8c887-0ffa-4031-855a-d88d3acb922d`)                                                                                                                                                                                                                                                                                                                                                                                                                     | Codex       |
| 2026-02-25  | Supabase security remediation (`20260225143500_enable_rls_on_public_policy_tables.sql`)              | User-confirmed Security Advisor state: "NO errors detected now" after migration applied on project `nkgscksbgmzhizohobhg`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Codex       |
| 2026-03-17  | Enrichment modal scroll reliability fix (`BUG-2026-02-24-ENRICH-SCROLL`)                             | Root cause identified and fixed: (1) `pr-4` was on `ScrollArea` wrapper, not on content div — scrollbar was overlapping the suggestion cards' action buttons. (2) Nested `ScrollArea h-[44vh]` inside `div.flex-1.overflow-y-auto` created double-scroll context making bottom buttons unreachable without scrolling within a viewport-constrained inner area. Fix: removed `ScrollArea` entirely from the suggestion list; `EnrichExperienceModal.tsx` outer `flex-1 overflow-y-auto` div now handles all scrolling. Removed unused `ScrollArea` import. Same `pr-*`-on-wrapper bug found and fixed in `ProfileImportReviewModal.tsx` (moved `pr-3` to content div). | Claude Code |
| 2026-03-27  | P18 CV Optimisation Score — S1 migration `20260317160000` (skill_experience_id FK)                   | `sats_enriched_experiences.skill_experience_id` column confirmed present and nullable via Supabase REST API (`GET /rest/v1/sats_enriched_experiences?select=id,skill_experience_id` → HTTP 200, `skill_experience_id: null`). Required test was "Confirm column present" — confirmed.                                                                                                                                                                                                                                                                                                                                                                                 | Claude Code |
| 2026-03-27  | Placeholder-state UX pass (grey-out non-implemented sections/buttons in Dashboard + Admin Dashboard) | Code review 2026-03-27: `Dashboard.tsx` and `AdminDashboard.tsx` contain no disabled/placeholder UI sections. No blocking code found. Item removed as release blocker; any future placeholder UX work to be tracked as a product backlog item.                                                                                                                                                                                                                                                                                                                                                                                                                        | Claude Code |
| YYYY-MM-DD  | Change name                                                                                          | test report / SQL verification / logs                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Name        |
