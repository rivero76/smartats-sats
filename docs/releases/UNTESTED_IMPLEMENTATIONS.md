# Untested Implementations and Release Blockers

## Purpose
Track changes that are implemented but not fully validated. Any item listed here is a release blocker for end-user rollout until required tests are completed.

## Rule
1. If implementation is complete but testing is partial/incomplete, add an entry here immediately.
2. Mark release status as `BLOCKED`.
3. Remove or close the entry only after required tests pass.

## Open Blockers
| Date | Change | Status | Missing Tests | Owner | Required Before Release |
|---|---|---|---|---|---|
| 2026-02-24 | P14 Story 1 (`fetch-market-jobs`) deployment | CODE-VERIFIED — runtime E2E pending | Code review (2026-03-17): `fetch-market-jobs/index.ts` correctly stages mock jobs to `sats_job_descriptions` with status `staged`, deduplicates by URL hash, and logs run via centralized logging. Structure is sound. | Claude Code | Confirm cron runs, validate staged row lifecycle and dedupe outcomes over repeated runs |
| 2026-02-24 | P14 Story 2 (`async-ats-scorer`) deployment | CODE-VERIFIED — runtime E2E pending | Code review (2026-03-17): `async-ats-scorer/index.ts` pulls `staged` jobs, builds baseline from `sats_user_skills`/`sats_skill_experiences`, calls `callLLM()` with ATS schema-locked output, materialises `sats_job_descriptions` + `sats_analyses` rows, marks processed/error status. Uses `_shared/` utilities. Idempotency logic present. | Claude Code | Run E2E test pass and confirm logs + DB outputs under real JWT/session context |
| 2026-02-24 | P14 Story 3 (threshold + notifications) deployment | CODE-VERIFIED — runtime E2E pending | Code review (2026-03-17): scorer references `profiles.proactive_match_threshold` with 0.60 global fallback; `sats_user_notifications` insert is conditional on score ≥ threshold; dedupe guard present. | Claude Code | Run E2E tests confirming only above-threshold matches notify and duplicates are prevented |
| 2026-02-24 | P14 Story 4 (`/opportunities` UI dashboard) implementation | CODE-VERIFIED — runtime E2E pending | Code review (2026-03-17): `ProactiveMatches.tsx` queries `sats_analyses` joined to proactive `sats_job_descriptions`, filters by score ≥ threshold, sorts by score desc then date, renders cards with score/missing skills/source URL. | Claude Code | Validate dashboard cards against seeded/real proactive analyses and confirm ordering + content rendering |
| 2026-02-24 | ATS analyses auto-refresh UX improvement (processing state stays at 60% until manual refresh) | CODE-VERIFIED — runtime E2E pending | `useATSAnalyses.ts` code review (2026-03-17): `refetchInterval` polling at 3s when any analysis is `initial/queued/processing` is correctly implemented; Supabase realtime channel on `sats_analyses` with `user_id` filter correctly invalidates TanStack Query cache on change; `ATSAnalysisProgress.tsx` correctly maps `processing→60%` and `completed→100%`; live-status indicator and last-sync timestamp render in `ATSAnalyses.tsx`. Implementation is code-correct. | Claude Code | Confirm in running app that processing analyses auto-transition to completed state without reload under normal and delayed realtime scenarios |
| 2026-02-25 | P15 Story 1 (`sats_learning_roadmaps` + `sats_roadmap_milestones` schema and RLS) | CODE-VERIFIED — runtime E2E pending | Code review (2026-03-17): migration file `20260225001000_add_p15_upskilling_roadmap_schema.sql` defines both tables with `user_id` FK and RLS policies for owner-only CRUD. Schema matches `generate-upskill-roadmap` persistence logic. | Claude Code | Apply migration (confirm already applied to project `nkgscksbgmzhizohobhg`); run cross-tenant denial checks |
| 2026-02-25 | P15 Story 2 (`generate-upskill-roadmap` edge function) implementation + deployment | CODE-VERIFIED — runtime E2E pending | Code review (2026-03-17): `generate-upskill-roadmap/index.ts` accepts `missing_skills` + `target_role`, calls `callLLM()` with schema-locked ROADMAP_JSON_SCHEMA, inserts roadmap + milestones into `sats_learning_roadmaps`/`sats_roadmap_milestones`, returns `roadmap_id`. Uses `_shared/` utilities. JSON schema enforces `project` milestone type. | Claude Code | Run E2E call from authenticated UI/session; verify `roadmap_id` returned; confirm milestone order and project milestone present |
| 2026-02-25 | P15 Story 3 (`/roadmaps` timeline UI + milestone completion toggles + progress bar) implementation | CODE-VERIFIED — runtime E2E pending | Code review (2026-03-17): `UpskillingRoadmap.tsx` renders milestones in `order_index` sequence with type icons, completion toggles update `is_completed` in DB, progress formula is `(completed/total)*100`. Empty/loading states present. | Claude Code | Validate with real generated data: toggle milestones, confirm `is_completed` persists, verify progress percentage across refresh |
| 2026-02-25 | P13 Story 1 (`linkedin-profile-ingest` edge function) implementation | CODE-VERIFIED — runtime E2E pending | Code review (2026-03-17): `linkedin-profile-ingest/index.ts` calls Playwright scraper via HTTP (not mock), passes result through `callLLM()` with schema-locked JSON, returns structured preview without any DB writes. Uses `_shared/` utilities correctly. | Claude Code | Invoke with valid LinkedIn URL under authenticated user; verify returned arrays + provenance fields; confirm no DB mutations |
| 2026-02-25 | P13 Story 2 (frontend merge/dedupe utility + import preparation hook) implementation | CODE-VERIFIED — runtime E2E pending | Code review (2026-03-17): `linkedinImportMerge.ts` implements canonicalization + Levenshtein fuzzy threshold for skills; experience fingerprint dedup is correct (asymmetry bug fixed 2026-03-01); provenance fields tagged on all insert-ready rows; `skills_to_insert/merge/ignore` + `experiences_to_insert/ignored` buckets produced correctly. | Claude Code | Execute Story 1 preview → Story 2 preparation flow; verify bucket decisions and provenance values under real baseline data |
| 2026-03-01 | P13 Story 3 (HITL Review UI — `ProfileImportReviewModal.tsx` + Settings import button) | CODE-VERIFIED — runtime E2E pending | Code review + scroll fix (2026-03-17): all acceptance criteria met in code. Checkboxes default-checked; `Approve and Save` writes only selected items; loading/error/success states present; empty-state renders when no new items found. `Settings.tsx` import button correctly invokes edge function → merge prep → modal. Scroll clipping bug fixed (pr-3 moved to content div). ~~Migration applied 2026-03-01~~ ✓ | Claude Code | Run E2E flow with real LinkedIn URL; confirm DB row insertions match checkbox selection; verify rejected items produce no DB writes |
| 2026-02-25 | Help Hub page (`/help`) UI implementation | BLOCKED | Manual navigation and usability validation: sidebar entry, route render, topic search/filter behavior, and deep-link navigation to related pages | TBD | Verify `/help` loads for authenticated users, search results are accurate, and each actionable topic button routes correctly |
| 2026-02-25 | Placeholder-state UX pass (grey-out non-implemented sections/buttons in Dashboard + Admin Dashboard) | BLOCKED | Manual UI validation for visual states, disabled interactions, and readability/accessibility of preview/planned sections | TBD | Confirm non-implemented controls remain non-actionable, visual affordance is clear, and no regression in functional tabs (notably admin Logging Control) |
| 2026-03-17 | P16 S1 — Resume Persona Model (`sats_resume_personas` migration + `useResumePersonas` hook + `PersonaManager` component wired into Settings) | CODE-VERIFIED — runtime E2E pending | Migration `20260317120000_p16_s1_create_resume_personas.sql` applied. `PersonaManager` renders in Settings. CRUD operations via `useResumePersonas` hook. Requires runtime validation: create persona, edit persona name/target role, set active, delete with confirmation dialog. Confirm RLS isolates personas per user (cross-tenant denial test). | Claude Code | E2E Settings flow with authenticated user; confirm DB rows in `sats_resume_personas`; RLS denial test |
| 2026-03-17 | BUG-2026-03-17-LOCATION-RLS fix — `sats_locations` + `sats_companies` SELECT policies replaced | CODE-VERIFIED — migration applied | Migration `20260317150000_fix_locations_companies_select_policy.sql` applied to project `nkgscksbgmzhizohobhg`. Requires runtime confirmation that Job Description creation with a new location (e.g. "São Paulo, São Paulo, Brazil") no longer throws "new row violates row-level security policy". | Claude Code | Create a Job Description with a new location via URL ingestion; confirm toast shows "Location created" (not error); confirm location row exists in `sats_locations` |
| 2026-03-17 | P18 CV Optimisation Score — S1 migration `20260317160000` (skill_experience_id FK) | CODE-VERIFIED — migration applied | Migration applied to `nkgscksbgmzhizohobhg`. Verify `skill_experience_id` column exists on `enriched_experiences` and accepts NULL without error. Cross-tenant denial test not required (existing RLS policy covers the table). | Claude Code | Confirm column present via Supabase table editor |
| 2026-03-17 | P18 CV Optimisation Score — S2 ATS scorer enrichment projection | CODE-VERIFIED — runtime E2E pending | Two-call isolation implemented (commit `bf879f9`): baseline `buildATSPrompt()` is pure (no enrichments); `getAcceptedEnrichments()` runs after baseline call; separate `callLLM()` with `CV_OPTIMISATION_JSON_SCHEMA` and `buildOptimisationPrompt()` projects enriched score. Optimisation failure is isolated and cannot corrupt `ats_score`. Requires runtime test: (1) create ≥1 accepted enrichment on a resume, (2) run new ATS analysis against a JD, (3) confirm `analysis_data.cv_optimisation_score` and `analysis_data.enrichments_used_count > 0` in DB; (4) confirm baseline score is NOT lower than a pre-enrichment run on same resume+JD. | Claude Code | Run ATS analysis with accepted enrichments; confirm `analysis_data` contains `cv_optimisation_score`, `cv_optimisation_improvements`, and `enrichments_used_count > 0`; confirm baseline ats_score is unchanged vs a run without enrichments |
| 2026-03-17 | P18 CV Optimisation Score — S3 UI panel in ATSAnalyses.tsx | CODE-VERIFIED — runtime E2E pending | Green optimisation panel renders when `analysis_data.cv_optimisation_score != null && enrichments_used_count > 0`. Shows projected %, delta badge, improvement list. Hidden when no enrichments were used. | Claude Code | After E2E scorer test above, confirm panel renders on the analysis card with correct values; confirm panel is absent for analyses run without enrichments |
| 2026-03-17 | Admin LogViewer time-window filter (`src/components/admin/LogViewer.tsx`) | CODE-VERIFIED — runtime E2E pending | Time window dropdown (Last 5 min / 15 min / 1h / 6h / 24h / All) added. Default: ERROR + Last 1h. Requires runtime validation that the filter correctly constrains results returned from `log_entries`. | Claude Code | Open Admin → Logging Control → Log Viewer; confirm default shows ERROR/1h; switch to All Levels/All time and verify row count increases; switch to Last 5 min and verify recency constraint |

## Closure Template
| Date Closed | Change | Evidence | Closed By |
|---|---|---|---|
| 2026-02-24 | Enrichment edge compatibility fix (`enrich-experiences`) | Deployed to project `nkgscksbgmzhizohobhg`; direct function invocation returned `HTTP 200` and `success: true` with suggestions (`request_id=manual-test-20260224c`) | Codex |
| 2026-02-24 | Enriched experience soft-delete RLS fix (delete flow stability) | User-verified logs show repeated `enrichment.deleted` success events with no new RLS failures in latest window (`request_id`s include `enrich-delete-ee15b057-7a86-4601-a5a5-dfc7b012ceff`, `enrich-delete-26b8c887-0ffa-4031-855a-d88d3acb922d`) | Codex |
| 2026-02-25 | Supabase security remediation (`20260225143500_enable_rls_on_public_policy_tables.sql`) | User-confirmed Security Advisor state: "NO errors detected now" after migration applied on project `nkgscksbgmzhizohobhg` | Codex |
| 2026-03-17 | Enrichment modal scroll reliability fix (`BUG-2026-02-24-ENRICH-SCROLL`) | Root cause identified and fixed: (1) `pr-4` was on `ScrollArea` wrapper, not on content div — scrollbar was overlapping the suggestion cards' action buttons. (2) Nested `ScrollArea h-[44vh]` inside `div.flex-1.overflow-y-auto` created double-scroll context making bottom buttons unreachable without scrolling within a viewport-constrained inner area. Fix: removed `ScrollArea` entirely from the suggestion list; `EnrichExperienceModal.tsx` outer `flex-1 overflow-y-auto` div now handles all scrolling. Removed unused `ScrollArea` import. Same `pr-*`-on-wrapper bug found and fixed in `ProfileImportReviewModal.tsx` (moved `pr-3` to content div). | Claude Code |
| YYYY-MM-DD | Change name | test report / SQL verification / logs | Name |
