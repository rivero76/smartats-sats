# Changelog

All notable changes to this project should be documented in this file.

## [Unreleased]

### Fixed

- 2026-04-08: Marketing website — created `marketing/` directory as a standalone Vercel static site. Fixed all 19 placeholder `href="#"` links in the landing page (CTAs → app auth, footer → real destinations), fixed billing copy conflict in FAQ, wired investor page footer. Ready to deploy as a separate Vercel project (Root Directory: `marketing`).

- 2026-04-08: Brazilian testing backlog — added P0-6 (Supabase Auth emails in pt-BR), P0-7 (ATS prompt for Brazilian CV format), P0-8 (UTF-8 smoke test), P0-9 (DD/MM/YYYY date parsing) as P0 items in TECHNICAL_IMPROVEMENTS.md. P0-4 (cross-language skill normalization) and P0-5 (LGPD) were added earlier in this session.

- 2026-04-08: P1-14 — Deletion confirmation email implemented in `delete-account` edge function via Resend (`RESEND_API_KEY` secret + `SATS_APP_URL`). Email includes permanent deletion date and a cancel link. Non-fatal — provider errors are logged but do not block the deletion flow.

- 2026-04-08: Delete Account was failing with "Edge Function returned a non-2xx status code" — three root causes fixed. (1) `supabase.auth.admin.signOut` was called with `user.id` (a UUID) instead of `token` (the user's JWT) — admin.signOut expects a JWT and threw "invalid number of segments". (2) `soft_delete_user`, `cancel_account_deletion`, and `reactivate_soft_deleted_user` PL/pgSQL functions still referenced `public.account_deletion_logs`, renamed to `sats_account_deletion_logs` in P21. Migration `20260408000000` patches the `account_deletion_logs` reference; migration `20260408010000` patches the `enriched_experiences` reference (also renamed in P21 to `sats_enriched_experiences`, missed in the first pass).

- 2026-04-07: WAF CRITICAL fixes — `ats-analysis-direct` returns 503 (not 500) for missing OPENAI_API_KEY. `delete-account` and `cancel-account-deletion` validate SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY before use and return 503 on misconfiguration, preventing unhandled crashes from `!` force-unwrap on absent env vars.
- 2026-04-07: RLS verified on P28 tables — `sats_profile_fit_reports` (owner-scoped) and `sats_feature_flags` (admin write-only) confirmed correctly configured.

### Added

- 2026-04-07: `docs/advisory/2026-04-07_saas-podcast-advisory-guide.md` — Saved the full SmartATS Founder's Advisory Guide: 7 sessions with Omer Khan and SaaS Podcast guests, one per product lifecycle phase (Validation → Pricing → First 10 → Activation → Retention → Growth → Enterprise). Each phase includes anchor episodes with direct URLs, [ASK OMER] and [ASK THE GUEST] question sets specific to SmartATS, the one question Omer always asks that founders avoid, and the biggest mistake founders make at that phase. Sourced from live research across 477+ saasclub.io episodes.

- 2026-04-07: `docs/decisions/ROADMAP.md` — New visual product roadmap in professional PM format. Companion to the dense technical `product-roadmap.md`. Includes: product vision statement, tier table, status legend (✅/🔄/🚧/🔒/💡), phase cards with problem/solution/done-when/agent-flow, quarterly OKRs, and references table. Designed to be the primary strategic reference updated whenever phase status changes.

- 2026-04-07: `docs/conventions/plan-conventions.md` — New plan format standard. Defines the required sections for every plan in `plans/`: header with status badge, Advisory Checkpoint (saas-advisor findings before implementation), Agent Execution Sequence checklist (before/during/after), Success Metrics, Stories, Technical Risks, Out of Scope, References. Includes the minimal viable plan template. Also defines when to consult saas-advisor, the Future agent slots pattern, and the plan status lifecycle.

- 2026-04-07: `CLAUDE.md` — Added `## Product Planning Standards` section: visual roadmap vs. technical roadmap distinction, plan format mandate (plan-conventions.md), saas-advisor consultation rules, advisory guide reference, and instructions for registering new agents. Updated PM-to-developer flow to include `saas-advisor` as the first step.

- 2026-04-07: New `.claude/agents/saas-advisor.md` agent — SaaS product advisor modelled on Omer Khan (_The SaaS Podcast_) and guest founders. Uses `WebSearch` + `WebFetch` to find relevant saasclub.io episodes, reads the SmartATS product vision and roadmap for context, and applies compound SaaS founder wisdom to strategic product questions (pricing, positioning, ICP, onboarding, launch sequencing, feature prioritisation). Registered in CLAUDE.md agent table under Advisory phase.

- 2026-04-07: CLAUDE.md — Added new `### UI/UX Design Principles` section under `### Frontend Patterns` documenting 6 design principles: progressive disclosure (expand/collapse UX for data lists), score-first hierarchy (visual hero pattern for key metrics), colour-coded status borders (green/amber/red per performance tier), filter bars on data list pages, engineering metadata in debug views only, AnimatePresence for smooth expand/collapse animations. Principles derived from Apple HI guidelines and applied to ATS Analyses redesign.

- 2026-04-07: `src/hooks/usePlanFeature.ts` — Added new `PlanFeatureKey` `'ats_score_breakdown'` (Pro+ tier) to both the union type definition and the `PLAN_FEATURES` capability map. Enables plan-tier gating of ATS Debug Modal's Score Breakdown section.

- 2026-04-07: Per-user plan override + database-driven feature flags (PM self-service). New migration `20260407100000_add_plan_override_to_profiles.sql` adds `plan_override TEXT CHECK (IN 'free','pro','max','enterprise')` column to `profiles` table with admin SELECT/UPDATE RLS policies. New migration `20260407110000_create_sats_feature_flags.sql` creates `sats_feature_flags` table with `(feature_key, plan_tier, is_enabled)` schema, seeded with 33 rows from current PLAN_FEATURES map; RLS policies allow authenticated users SELECT and admins INSERT/UPDATE/DELETE. New `src/hooks/useFeatureFlags.ts` hook fetches flags table with 5-min staleTime, returns `Map<PlanFeatureKey, Set<PlanTier>>`, exports `FEATURE_FLAGS_QUERY_KEY` for cache invalidation. Updated `src/hooks/usePlanFeature.ts` to read `profiles.plan_override` per-user; resolves effective plan as `plan_override ?? 'free'`; uses `useFeatureFlags()` for live flag lookup with hardcoded `PLAN_FEATURES` fallback if DB unavailable. New `src/components/admin/FeatureFlagsPanel.tsx` admin panel: toggleable grid (feature × tier), Switch per cell, Save with upsert and cache invalidation. New `src/components/admin/PlanOverridePanel.tsx` admin panel: searchable user table, per-row plan override dropdown, Save button. Updated `src/pages/AdminDashboard.tsx` with "Feature Flags" (Flag icon) and "Plan Overrides" (UserCog icon) tabs. Interface remains stable for P22 billing integration.

- 2026-04-07: P28 S1–S7 — LinkedIn Profile Fit Analyzer fully implemented. New `sats_profile_fit_reports` table (migration `20260407000000`, owner-only RLS, immutable). New `analyze-profile-fit` edge function: deterministic 0–100 fit score (weighted signal coverage), LLM rationale + per-gap recommended actions via `OPENAI_MODEL_PROFILE_FIT` (`gpt-4.1-mini`), optional resume reconciliation LLM call when `resume_id` provided. New `useProfileFit.ts` hook (`useProfileFitHistory` query + `useRunProfileFit` mutation with `PlanGateError`). `usePlanFeature` extended with `profile_fit` (Pro+) and `profile_fit_reconciliation` (Max+) keys. New `/profile-fit` route, `ProfileFit.tsx` page (fit score card, gap breakdown, reconciliation section, history chart), `ReconciliationConflictList.tsx`, `FitScoreHistoryChart.tsx` sub-components. `Target` icon added to sidebar nav. Playwright scraper enriched with certifications array and recommendation_count integer (S7); `linkedin-profile-ingest` edge function updated to forward and normalize certifications. `OPENAI_MODEL_PROFILE_FIT` registered in LLM model governance doc.

- 2026-04-07: P28 — LinkedIn Profile Fit Analyzer plan created. `plans/p28-linkedin-profile-fit-analyzer.md` decomposed into 8 stories: S1 DB foundation (`sats_profile_fit_reports` table + RLS), S2 `analyze-profile-fit` edge function (profile vs. market signals, 0–100 fit score, optional resume reconciliation), S3 `useProfileFit` TanStack Query hook + `profile_fit` / `profile_fit_reconciliation` feature keys in `usePlanFeature`, S4 `/profile-fit` route and core UI (tier-gated score display + gap breakdown), S5 reconciliation view (Max+), S6 historical score tracking chart (Max+), S7 Playwright scraper enrichment (certifications + recommendation count), S8 help/marketing updates. Model: `OPENAI_MODEL_PROFILE_FIT` (default `gpt-4.1-mini`). Tier gating: Free (score only), Pro+ (breakdown + recommendations), Max+ (reconciliation + history).

- 2026-04-07: P22 S2 — Plan billing tier UI component `src/components/settings/PlanBillingCard.tsx`. Displays current plan tier with colour-coded badge (Free gray, Pro blue, Max purple, C-Level gold). Four plan tier comparison cards with price ($0/$19/$39/$99), description, and feature bullet list; current tier highlighted with "Current plan" badge and disabled CTA. Pro tier displays "Most popular" badge. Free and Pro tiers show "Upgrade" button that opens dialog explaining billing is coming soon with "Request early access" mailto CTA (mailto to first email in `BILLING_CONTACT_EMAILS`). Max tier shows "Upgrade" with same early-access flow. C-Level tier shows "Contact sales" mailto CTA (mailto to first email in `BILLING_CONTACT_EMAILS` with subject prefix). Resolves UX dead-end: "Upgrade" buttons from locked feature pages now redirect to Settings where users see actionable tier comparison and contact options instead of empty screen. Added to `src/pages/Settings.tsx` as first section above existing setting cards.

- 2026-04-02: Dev reset script `scripts/ops/dev-reset.sh`. Dry-run shows row counts across all career tables + auth account count. `--confirm` wipes all career data (analyses, JDs, resumes, skills, roadmaps, enrichments, personas, notifications, staged jobs) against the linked Supabase project; requires typing the project ref to proceed. `--list-users` shows all auth accounts with role column to identify which to delete manually. Hard-coded project ref guard aborts if run against an unrecognised project.

- 2026-04-02: Gap 1 (P25 S2) — `classify-skill-profile` edge function confirmed shipped (written 2026-03-30). Schema-locked LLM classification (temperature=0, seed=42), career chapter inference, diff mode for re-ingestion, graceful fallback. Model `OPENAI_MODEL_SKILL_CLASSIFY` registered in governance doc. No code changes — gap was already closed.

- 2026-04-02: Cleanup migrations for P14 test contamination. `20260402020000_cleanup_orphaned_proactive_data.sql` — initial cleanup pass (reset staged jobs to queued; no JDs deleted because test accounts still exist in auth.users). `20260402030000_cleanup_mock_proactive_data.sql` — correct filter: deleted all `sats_job_descriptions`, `sats_analyses`, and `sats_staged_jobs` where `source_url LIKE 'https://example.com/%'` (3 mock jobs × 6 auth accounts = 18 ghost JDs removed). Verified: 7 real JDs remain, all `count=1`, no duplicates.

- 2026-04-02: Gap 2 (Admin JD Observability) — Admin-only cross-user job description panel. Migration `20260402000000_admin_jd_select_policy.sql` adds `has_role(auth.uid(), 'admin'::app_role)` SELECT policy on `sats_job_descriptions`. New `src/components/admin/AdminJobDescriptionsPanel.tsx` component: filterable table (search, source type, date range) showing all JDs across all users up to 500 rows. New "Job Descriptions" tab added to `/admin` dashboard alongside existing Overview and Logging tabs.

- 2026-04-02: Gap 3 (Feature Gating) — New `src/hooks/usePlanFeature.ts` hook. Defines 9 feature keys (`skill_reclassify`, `skill_reclassify_all`, `proactive_matching`, `linkedin_import`, `cv_optimisation`, `ai_roadmap`, `ats_analysis_unlimited`, `byok`, `model_selection`) with `PLAN_FEATURES` capability map per tier. Returns `{ plan, isLoading, hasFeature(key) }`. Defaults to `'free'` for all users until P22 billing ships — hook interface is stable for P22 to plug real subscription data in. Wired into `SkillProfileManager`: delete button is now disabled for Free tier users (gated to Pro/Max); "Re-classify all" placeholder button added and gated to Pro/Max.

- 2026-04-02: Gap 4 (P20 S4 Career Data Reset) — Full career data reset feature. Migration `20260402010000_p20_s4_reset_career_data_rpc.sql` adds `reset_career_data(user_id)` SECURITY DEFINER RPC that hard-deletes resumes, analyses, JDs, enrichments, skills, roadmaps, personas, and notifications; logs to `sats_account_deletion_logs`; preserves auth, profiles, and settings. New `supabase/functions/reset-profile-data/index.ts` edge function (thin JWT-validated HTTP wrapper). New `src/hooks/useCareerDataReset.ts` mutation hook (calls edge function, invalidates all user caches on success). New `src/components/ResetCareerDataModal.tsx` modal with typed "RESET" confirmation (no password required). Wired into `src/pages/Settings.tsx` Data Management card between Export and Delete Account.

- 2026-04-05: P26 — Gap Analysis Engine. Full implementation across 7 phases. Phase 0 migrations: `sats_role_families` (88 curated role families with regional aliases across 11 domains), `sats_market_signals`, `sats_gap_snapshots`, `sats_gap_items`, profile career goals columns (`target_market_codes`, `primary_target_role_family_id`), skill profile cert status columns. New edge functions: `aggregate-market-signals` (nightly scheduled aggregation of job posting signals into frequency data by role/market/window), `generate-gap-matrix` (on-demand per-user gap computation: market signals vs. candidate profile, LLM-enriched recommendations). Extended `async-ats-scorer` with job signal extraction (certifications, tools, methodologies, seniority, market code, role family) via `gpt-4.1-mini`. Extended `generate-upskill-roadmap` to accept `gap_snapshot_id` as alternative input — derives target role and missing skills from gap items automatically. New frontend: `/gap` route and `GapMatrix.tsx` page (Pro+ gated: three collapsible gap tiers, market/role selectors, refresh CTA, roadmap generation from gaps); `CareerGoalsCard` in Settings; `useGapAnalysis`, `useCareerGoals`, `useRoleFamilies` hooks. Certification status inline editing in `SkillProfileManager` (Pro+ only). `usePlanFeature` extended with `gap_analysis` key. `useGenerateRoadmap` mutation added to `useUpskillingRoadmaps`.

- 2026-04-05: ADR-0007 RUNTIME-VERIFIED — Postmark inbound email pipeline fully operational end-to-end. Two bugs fixed during live testing: (1) `verify_jwt=false` added to `supabase/config.toml` for `inbound-email-ingest` (Postmark is external and cannot provide a JWT — was returning 401); (2) LinkedIn job alert emails use `/comm/jobs/view/JOBID` tracking URLs, not `/jobs/view/JOBID` — both HTML anchor and plain-text regexes updated to handle `(?:comm\/)?` path prefix. Live test result: 1 forwarded LinkedIn alert → 5 jobs staged → scorer run → `processed_jobs:6, scored_analyses:6, notifications_triggered:4` → `/opportunities` populated. Full session notes in `docs/sessions/opportunities-email-ingestion-2026-04-05.md`.

- 2026-04-02: ADR-0007 — Email inbound job ingestion via Postmark. New `supabase/functions/inbound-email-ingest/index.ts` edge function: parses forwarded LinkedIn job alert emails and generic job board emails (Seek, Indeed, Lever, Greenhouse) into `sats_staged_jobs` for pickup by `async-ats-scorer`. Two security guards: (1) Postmark `X-Postmark-Signature` token verification against `postmark_webhook_secret` in `sats_runtime_settings` (skipped if empty for initial setup), (2) sender allowlist (`inbound_email_allowlist`) — unknown senders silently dropped with `200 OK` to prevent Postmark retries. LinkedIn parser: anchor regex extracts job ID, title, and company from bullet-separated context. Generic URL extractor handles Seek/Indeed/Lever/Greenhouse patterns. Content deduplication via `sha256` of normalized description; `23505` unique violations silently skipped. Migration `20260402040000_postmark_inbound_email_config.sql` seeds three keys into `sats_runtime_settings`: `postmark_webhook_secret` (empty), `inbound_email_allowlist` (empty), `inbound_email_ingest_url` (set). Settings page updated with full "Email Job Alerts" setup card: inbound address with copy button, Step-by-step Postmark onboarding instructions. ADR-0007 recorded in `docs/decisions/adr-0007-2026-04-02-email-inbound-job-ingestion.md`.

- 2026-04-02: PM Dashboard (`/pm` route). Created `src/pages/PMDashboard.tsx` that aggregates product management tracking into one unified view using Vite `?raw` imports of markdown source files. Sources aggregated: `docs/decisions/product-roadmap.md`, `docs/releases/UNTESTED_IMPLEMENTATIONS.md`, `docs/improvements/TECHNICAL_IMPROVEMENTS.md`, `docs/bugs/BACKLOG.md`, `docs/incidents/INCIDENTS.md`, `docs/changelog/CHANGELOG.md`, and all `plans/*.md` files. Dashboard displays summary stat cards (document counts) plus 7 tabs: Roadmap, Release Blockers, Tech Backlog (with search + filter), Bugs, Incidents, Plans, and Changelog. Each section includes source file path badge with click-to-copy clipboard functionality. In dev mode, Vite HMR auto-reloads the dashboard whenever any source markdown file changes (badge displays "Live (HMR)" indicator). Added `/pm` route to `src/App.tsx`. Added "PM Dashboard" navigation item with `ClipboardList` icon to `src/components/AppSidebar.tsx`.

### Changed

- 2026-04-07: `src/components/ATSDebugModal.tsx` — Redesigned with feature gating and Apple-level UX polish. Score Breakdown section now gated to Pro+ (`ats_score_breakdown`). CV Optimisation section gated to Pro+ (`cv_optimisation`). Model Details card (cost, tokens, request ID, model name) now hidden for Free/Pro tiers, shown only for Max+ (`byok` feature). Prompts tab newly gated to Max+ with lock icon in tab label and LockedTabContent nudge. AI Response tab renamed to "AI Output", gated to Max+. Tokens tab renamed to "Usage", gated to Max+. Retry Analysis button relocated to always-visible Analysis Status card. Score Breakdown bars now colour-coded (green for >= 70%, amber for 50–69%, red for < 50%) instead of always-blue. "Warning N" numbered labels removed from Errors tab. New private sub-components: `LockedTabContent` (placeholder + unlock prompt), `LockedPanel` (greyed-out card with padlock icon), `LockedTabTrigger` (tab label with lock icon).

- 2026-04-07: `src/pages/ATSAnalyses.tsx` redesigned with Apple-level UIUX approach. Refactored analysis cards from flat list to collapsed state with progressive disclosure via click-to-expand interactions. Colour-coded left border per analysis card indicates performance tier: green (score >= 80%), amber (60–79%), red (< 60%), blue (analysis in-progress). Signal badges (matched/missing skill counts) now visible in collapsed state for quick scanning. New filter bar added with four options: All / Strong Match (>= 80%) / Needs Work (< 80%) / In Progress. Debug and Export buttons relocated from collapsed card header to expanded state only, eliminating visual clutter. Smooth expand/collapse animations implemented using `AnimatePresence` and Framer Motion. Score percentage rendered as large visual hero (anchor element of each card) for immediate pattern recognition. Updated styling with `shadcn/ui` Collapsible component to maintain design system consistency.

### Fixed

- 2026-04-07: Fixed 401 Unauthorized error in `analyze-profile-fit` edge function (`supabase/functions/analyze-profile-fit/index.ts` line ~412). Corrected Supabase client initialization from incorrect pattern `createClient(supabaseUrl, userToken)` to correct pattern `createClient(supabaseUrl, supabaseAnonKey, { global: { headers: { Authorization: authHeader } } })`. Added UPDATE LOG entry.

- 2026-04-07: P28 S7 — Deployed missing `linkedin-profile-ingest` edge function changes to production (certifications array and recommendation_count forwarding). These changes were completed during P28 development but were omitted from the initial P28 deployment. Now production-synchronized.

- 2026-04-07: P28 runtime validation completed. Tests 3–8 all passed. Test 9 (scraper enrichment) is code-verified; pending Railway LinkedIn credentials for full runtime pass. `docs/releases/UNTESTED_IMPLEMENTATIONS.md` updated to RUNTIME-VERIFIED status as of 2026-04-07. Full results documented in `docs/sessions/p28-manual-validation-2026-04-07.md`.

- 2026-04-07: Reverted temporary test override in `src/hooks/usePlanFeature.ts` — plan tier restored to `'free'` after Test 8 browser testing completed.

### Changed

- 2026-04-07: `docs/audits/` restructured — separated prompt templates from generated reports. New `docs/audits/reports/` subdirectory holds all generated audit output (`YYYY-MM-DD_<type>.md`); 8 historical reports migrated there. `docs/audits/` root now contains only reusable prompt templates. Prompt templates (`ARCHITECTURE-DECISION-AUDIT.md`, `PM-PGM-AUDIT.md`, `aws-waf-review-prompt.md`, `code-review-prompt.md`) updated to point output paths at `docs/audits/reports/`. `AUDIT_LOG.md` updated to reflect new structure. `CLAUDE.md` Source of Truth table updated with `docs/audits/reports/` entry.

### Removed

- 2026-04-07: `docs/audits/` — 4 generated report files removed from the template root (`docs-structure-analysis-2026-03-18.md`, `job-seeker-gap-analysis-2026-03-27.md`, `linkedin-scraper-hosting-comparison-2026-04-01.md`, `product-strategy-audit-2026-03-27.md`); moved to `docs/audits/reports/`.

## [Unreleased]

### Added

- 2026-03-31: INFRA-1 — LinkedIn scraper migrated from Railway to Fly.io. Added `scripts/playwright-linkedin/fly.toml` (shared-cpu-1x, 1 GB RAM, auto-stop, health check at `/health`). Removed `railway.json`. Updated `Dockerfile`, `package.json`, `.env.example`, and `scraper.ts` to remove Railway references. Flagged as MVP-temporary in `docs/improvements/TECHNICAL_IMPROVEMENTS.md` — hosting strategy must be reviewed before scaling beyond MVP.
- 2026-03-31: AWS Well-Architected Framework review completed. Full report saved to `docs/improvements/CODE-REVIEW-2026-03-31.md`. WAF-1 through WAF-18 added to backlog. Verdict: BLOCKED on SEC-1 (direct OpenAI call in `linkedin-profile-ingest` bypassing `callLLM()`). Review prompt saved to `docs/audits/aws-waf-review-prompt.md` for future use.

### Added (continued)

- 2026-03-30: PROD-9 — Resume Format Audit intelligence. `ats-analysis-direct` edge function now calls LLM with INTELLIGENCE_JSON_SCHEMA to detect ATS-breaking patterns (tables, emojis, vague bullets, missing URL, length mismatch) with severity levels. Results stored as `format_audit` in `analysis_data` JSONB. New `OPENAI_MODEL_INTELLIGENCE` environment variable (default: `gpt-4.1-mini`) added to `docs/specs/technical/llm-model-governance.md`.
- 2026-03-30: PROD-10 — Resume Geography Mode intelligence. Added `target_country` parameter to `CreateATSAnalysisRequest`. User can select from 8 countries or use auto-detect. Detects target country from job description text or user override; produces per-country format checklist (photo, length, personal details, date format). Results stored as `geography_passport` in `analysis_data` JSONB. UI component `ATSAnalysisModal.tsx` now includes optional "Target Market" Select field.
- 2026-03-30: PROD-11 — Resume Industry Lens intelligence. Classifies job description by vertical (Tech, Finance, Healthcare, Legal, Creative, Academic, Startup, Operations); flags missing industry-expected resume sections. Results stored as `industry_lens` in `analysis_data` JSONB.
- 2026-03-30: PROD-12 — Resume Cultural Tone Advisor intelligence. Detects writing register and flags mismatches against target market cultural norms. Results stored as `cultural_tone` in `analysis_data` JSONB.
- 2026-03-30: Resume Intelligence display layer. Created `ResumeIntelligencePanel.tsx` component rendering four intelligence sub-sections (Format Audit, Geography Passport, Industry Lens, Cultural Tone) with null-safe rendering for pre-feature analyses. Imported and rendered on `/analyses` page after CV Optimisation panel. `useDirectATSAnalysis.ts` and `useATSAnalyses.ts` hooks updated to include `target_country` in request interface.
- 2026-03-30: Updated `ats-analysis-direct` edge function to run CV Optimisation (Call 2) and Resume Intelligence (Call 3) in parallel via `Promise.allSettled()`. IntelligenceResult type and buildIntelligencePrompt() utility added. Intelligence keys (format_audit, geography_passport, industry_lens, cultural_tone, target_country_input) stored in analysis_data JSONB.
- 2026-03-30: Updated `async-ats-scorer` batch analysis to store null for four intelligence keys (format_audit, geography_passport, industry_lens, cultural_tone) since batch-scored analyses do not invoke Resume Intelligence logic.
- 2026-03-30: P25 S1 — DB foundation for Skill Profile Engine. Created `sats_skill_profiles` table (user_id + skill_name UNIQUE, proficiency level, last_classified, career_chapter reference, tier gating) with RLS policies and audit trigger. Created `sats_skill_decay_config` table with 6 seeded decay rules (skill-specific exponential decay curves). Migration file: `supabase/migrations/20260330100000_p25_s1_skill_profiles.sql`.
- 2026-03-30: P25 S2 — `classify-skill-profile` edge function. Classifies skills from experience text using LLM with temperature=0, seed=42, and schema-locked output. Returns career chapters, transferable skills, and diff payload for re-ingestion mode. Graceful fallback on LLM errors. Added `OPENAI_MODEL_SKILL_CLASSIFY` environment variable entry to `docs/specs/technical/llm-model-governance.md`.
- 2026-03-30: P25 S3 — `SkillClassificationReview` transparency UI component + `useSkillProfile` TanStack Query hooks (query, upsert, delete). Pre-save report showing AI classification results with three-choice per-skill override options. Pro/Max-gated "explain" re-classification trigger. Framer Motion stagger animations for skill lists. Accessibility tests in place (5 new a11y tests).
- 2026-03-30: P25 S4 — `ats-analysis-direct` weighted skill injection. Implemented `buildWeightedSkillBlock()` function that reads user's skill profile + decay config at analysis call time, computes effective weights using decay formula, and injects as additive context into baseline ATS prompt.
- 2026-03-30: P25 S5 — Shared skill decay utility. Extracted decay logic into reusable `_shared/skillDecay.ts` module. Updated `async-ats-scorer` edge function to batch-read skill profiles + decay config once per run and inject per-user weighted context.
- 2026-03-30: P25 S6 — `SkillProfileManager` settings component. View skills grouped by career chapter, delete individual skills with confirmation dialog. Added to Settings page. Accessibility tests in place. Added `skillProfile` help topic to `src/data/helpContent.ts` with workflow guidance.

### Changed

- 2026-03-30: Updated `docs/specs/technical/llm-model-governance.md` — added `OPENAI_MODEL_INTELLIGENCE` to Model Register (task: "Resume intelligence (format audit, geography mode, industry lens, cultural tone)", default: `gpt-4.1-mini`). Added `OPENAI_MODEL_SKILL_CLASSIFY` to Model Register (task: "Skill profile classification from experience text", default: `gpt-4.1-mini`).

### Fixed

- 2026-04-01: UX-FILE-1 — Added cloud-file stub detection to catch OneDrive, Google Drive, iCloud, and Dropbox files that have not been downloaded locally. `src/services/documentProcessor.ts` now exports `assertFileIsLocal(file)` function that probes the first 16 bytes of a file and throws `FILE_NOT_LOCAL` error code if the read fails (indicating a cloud-only stub). Added `FILE_NOT_LOCAL` to `ProcessingError` enum. `src/components/FileUpload.tsx` and `src/components/JobDescriptionFileUpload.tsx` now call `assertFileIsLocal()` before attempting Supabase storage upload, surfacing an actionable error message: "This file appears to be stored only in the cloud (OneDrive, Google Drive, iCloud, or Dropbox) and has not been downloaded to your device. Open it in its app first to download it locally, then try uploading again." Replaces generic "Failed to fetch" errors.
- 2026-04-01: UX-FILE-1 (continued) — Added SPA shell detection and URL slug parsing to `supabase/functions/job-description-url-ingest/index.ts`. New `assessContentQuality()` function detects navigation-heavy SPA shells by checking for markers like "skip to main content" alongside absence of job content. New `extractTitleFromUrlSlug()` function parses URL path slugs (e.g., `manager-professional-services-8445817002` to "Manager Professional Services") for fallback job titles. Response now includes three fields: `content_quality: 'high' | 'low'`, `is_spa_shell: boolean`, `title_hint: string | null`. Extended `UrlIngestionResponse` interface in `src/hooks/useJobDescriptions.ts` with these fields. Updated `handleIngestFromUrl` to use `jsonld_job.title` as highest-priority title source, apply JSON-LD fields after heuristic extraction, and show SPA warning only when no JSON-LD fallback available. Added `KNOWN_BLOCKED_SITES` map (Seek, Indeed, Glassdoor) — when `blocked_host` matches a known blocker, shows site-specific guidance (e.g. "Seek actively blocks automated access. Open the job page, copy all the text, and use Paste Text."). User-visible impact: Workday jobs (Westpac, ANZ, big4 banks) now populate title/company/location/employment type correctly. Seek 403 now shows "Seek blocks automated access — copy and use Paste Text" instead of generic error. LinkedIn URL ingestion unchanged. Employer direct URLs with JSON-LD (Greenhouse, Lever, etc.) now work correctly.
- 2026-03-30: P14 S3 — Fixed `async-ats-scorer` notification threshold bug in `supabase/functions/async-ats-scorer/index.ts`. The `getUserThresholdMap()` function was using `DEFAULT_PROACTIVE_MATCH_THRESHOLD` (0.6 hardcoded) as fallback for users with null `proactive_match_threshold` in their profiles, causing the user threshold map to return 0.6 for all users. This prevented the `globalThreshold` from `sats_runtime_settings` (set to 0.40 for testing) from ever being reached. Fixed by removing the hardcoded fallback — `getUserThresholdMap()` now only inserts entries for users with an explicit non-null threshold. Users without one now correctly fall through to `globalThreshold` via the `??` operator. Code fix committed; function redeployment pending SUPABASE_ACCESS_TOKEN renewal.
- 2026-03-29: Added `@testing-library/dom` as direct dev dependency. The package was imported by axe-core tests but not declared, causing "Cannot find module" errors that failed 5 a11y tests. npm run verify now exits 0.
- 2026-03-29: Corrected `docs/specs/technical/llm-model-governance.md` — §2 Model Register production model is `gpt-4.1` (not stale `o4-mini` from 2026-03-17 rollback). §6 Change Log updated with rollback entry for historical accuracy.
- 2026-03-29: Fixed RLS cross-tenant test script bug in `scripts/ops/test-rls-cross-tenant.ts`: removed reference to non-existent `region` column in `sats_locations` table (table only contains city, state, country, created_at, updated_at).
- 2026-03-29: BUG-2026-03-17-LOCATION-RLS closed — cross-tenant RLS isolation on sats_locations confirmed working via runtime test (3/3 PASS).

### Changed

- 2026-03-29: Updated `docs/releases/UNTESTED_IMPLEMENTATIONS.md` — 8 items promoted from CODE-VERIFIED to RUNTIME-VERIFIED after Playwright functional test suite run. P16 S1 partial verification documented (6/7 persona manager tests pass, 1 skipped pending has_role migration). BUG-2026-03-17-LOCATION-RLS marked closed.

### Added

- P19 S4-1 — Bundle analyser: `rollup-plugin-visualizer` added to `vite.config.ts`; generates `dist/stats.html` treemap on every production build (gzip + brotli sizes). Lighthouse CI added as non-blocking `lighthouse` job in `quality-gates.yml` with category assertions (perf ≥ 0.7 warn, a11y ≥ 0.85 error, bp ≥ 0.8 warn). `lighthouserc.json` committed.
- P19 S5-1 — Storybook bootstrap: installed Storybook 8 (`@storybook/react-vite` + `addon-essentials` + `addon-a11y`). Config at `.storybook/main.ts` and `.storybook/preview.ts` (imports `src/index.css`, light/dark backgrounds). Six component story files: `Button` (10 stories), `Badge` (6), `Card` (4), `Input` (7), `Dialog` (3 — rendered open), `Table` (3). `storybook-static` and `.lighthouseci` added to `.prettierignore`.
- P19 S3-1 — axe-core a11y tests: 5 Vitest test files in `tests/unit/a11y/` (Dashboard, Resumes, JobDescriptions, ATSAnalyses, Settings). All pass with zero violations. Blocking in CI via `npm run test` step in `quality-gates.yml`. Setup utilities: `tests/unit/a11y/setup.tsx` (QueryClient + MemoryRouter wrapper) and `tests/unit/a11y/setup-dom.ts` (DOMMatrix stub for jsdom/pdfjs-dist compat).
- P19 S3-2 — Visual regression baselines: 5 PNG snapshots committed for Dashboard, Resumes, ATS Analyses, Enriched Experiences, Settings (`tests/e2e/visual/pages.spec.ts-snapshots/`). All 6 visual tests pass (auth setup + 5 pages). CI `visual-regression` job (non-blocking) already wired. Fixed `auth.setup.ts` ESM `__dirname` bug (`import.meta.url` + `fileURLToPath`).
- Functional Playwright E2E test suite: 6 new spec files (51 tests total) under `tests/e2e/` covering: Help Hub `/help` (10 tests), Opportunities `/opportunities` (8), ATS Analyses auto-refresh + CV opt panel (7), Roadmaps `/roadmaps` milestone toggle + persistence (8), Persona Manager CRUD in Settings (7), Admin LogViewer time-window filter (9). All skip gracefully without credentials. `playwright.config.ts` updated with `functional` project matching `e2e/*.spec.ts`.
- RLS cross-tenant denial test script `scripts/ops/test-rls-cross-tenant.ts`: programmatic test for P15 S1 roadmap isolation (User A creates → User B reads → expects 0 rows) and BUG-2026-03-17 locations INSERT path. Run with `npx tsx scripts/ops/test-rls-cross-tenant.ts`.
- `tests/unit/utils/linkedinImportMerge.test.ts` expanded from 3 → 11 tests: asymmetry-fix regression, provenance on experience inserts, empty baseline, within-payload duplicate skills/experiences, synonym canonicalization, below-threshold fuzzy, exact-match reason text. All pass.

### Fixed

- Disabled `DevErrorOverlay` (DEV LOGS panel) by setting `VITE_LOGGING_ENABLED=false` in `.env`. The overlay was rendering log entries at the bottom of the screen in development mode.
- `tests/e2e/visual/auth.setup.ts`: replaced `__dirname` with `fileURLToPath(import.meta.url)` to fix ESM incompatibility (`package.json` `"type":"module"` caused ReferenceError blocking all Playwright test listing).

- P21 S1 — Universal audit trigger function `set_audit_fields()` (SECURITY DEFINER). Auto-stamps `created_by`/`updated_by`/`version` on INSERT/UPDATE across all application tables. Migration: `20260327000000_p21_s1_universal_audit_trigger.sql`.
- P21 S1 — Added `created_by` and `updated_by` UUID columns to 19 tables with attached `trg_audit_<table>` triggers for automatic user tracking on mutations. Migration: `20260327100000_p21_s1_add_created_by_updated_by.sql`.
- P21 S1 — Added `deleted_by` UUID column to 13 soft-delete tables; patched `soft_delete_enriched_experience()` RPC to update `deleted_by` on record deletion. Migration: `20260327110000_p21_s1_add_deleted_by.sql`.
- P21 S1 — Added `deleted_at` + `deleted_by` + partial indexes to 3 tables missing soft-delete columns; recreated RLS policies with soft-delete guards. Migration: `20260327120000_p21_s1_add_missing_deleted_at.sql`.
- P21 S1 — Added `version` INT DEFAULT 1 column to 9 mutable tables for optimistic locking and concurrency control. Migration: `20260327130000_p21_s1_add_version_column.sql`.
- P21 S1 — Created `sats_llm_call_logs` table with comprehensive LLM observability schema: model used, tokens consumed (prompt/completion/total), cost estimate, latency, finish reason, task label, and user correlation. 3 indexes (user_id, task_label, created_at). RLS policies (owner-only read, admin metrics). Migration: `20260327140000_p21_s1_llm_call_logs.sql`.
- P21 S1 — Tier-1 table rename to universal `sats_` convention: `enriched_experiences` → `sats_enriched_experiences`, `log_entries` → `sats_log_entries`, `log_settings` → `sats_log_settings`, `account_deletion_logs` → `sats_account_deletion_logs`, `log_cleanup_policies` → `sats_log_cleanup_policies`, `user_roles` → `sats_user_roles`. Updated RPCs `run_log_cleanup_policies()` and `soft_delete_enriched_experience()` with new table references. Fixed admin policy on `sats_llm_call_logs`. Migration: `20260327150000_p21_tier1_rename_tables.sql`.
- P21 S1 — Wired `llmProvider.ts` to `sats_llm_call_logs`: added optional `logContext` field to `LLMRequest` and `finishReason` to `LLMResponse`; fire-and-forget insert executes after every successful LLM call with user_id, model, token counts, cost, duration, and task label.
- P21 S1 — Extended `docs/conventions/coding-conventions.md` with legacy `ats_*` table exceptions (7 tables documented); added Section 8 (SQL Function Naming: `sats_<verb>_<noun>()` pattern); added Section 9 (Trigger Naming: `sats_update_<table>_updated_at` / `trg_audit_<table>`).

- P21 S2 — RBAC infrastructure: created `sats_roles`, `sats_permissions`, `sats_role_permissions`, `sats_user_role_assignments` tables with full FK/index structure, RLS policies enforcing admin-only writes. Implemented `sats_has_permission(user_id UUID, permission_code TEXT)` function. Backfilled from legacy `sats_user_roles` one-time migration. Migration: `20260327200000_p21_s2_rbac_tables.sql`.
- P21 S2 — API key infrastructure: created `sats_api_keys` table with hash-only storage (keys never persisted raw), scoped to tenant, with rotation/expiry fields and RLS owner-only access. Migration: `20260327210000_p21_s2_api_keys.sql`.
- P21 S2 — Unified audit logging: created append-only `sats_audit_logs` table with UPDATE/DELETE policies blocked at database level. Implemented `sats_log_audit_event()` trigger firing on mutation (INSERT/UPDATE/DELETE) of `sats_analyses`, `sats_resumes`, `sats_enriched_experiences`, and `sats_api_keys`. Logs action, old/new values, user/IP, and timestamp. Migration: `20260327220000_p21_s2_unified_audit_logs.sql`.
