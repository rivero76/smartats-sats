# ── Required ──────────────────────────────────────────────────────────────────

# Shared secret between the Supabase edge function and this service.
# Generate a strong random string: openssl rand -hex 32
PLAYWRIGHT_API_KEY=your-strong-random-secret-here

# LinkedIn account credentials used for browser login.
# Use a dedicated account — not your main personal account.
LINKEDIN_EMAIL=you@example.com
LINKEDIN_PASSWORD=yourpassword

# ── Optional ──────────────────────────────────────────────────────────────────

# Base64-encoded JSON of saved LinkedIn session cookies.
# The service logs this value after the first successful login.
# Set it here to survive service restarts without re-logging in.
# LINKEDIN_COOKIES=

# Port the HTTP server listens on. Railway sets this automatically.
# PORT=3000
